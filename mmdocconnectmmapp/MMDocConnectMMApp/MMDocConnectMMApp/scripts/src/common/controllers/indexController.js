"use strict";

define(['angularAMD', 'alertsServices'], function (app) {
    app.controller('indexController', ['$scope', '$rootScope', 'ngDialog', '$timeout', '$location', '$filter', 'alertsServices', function ($scope, $rootScope, ngDialog, $timeout, $location, $filter, alertsServices) {
        //$scope.CloseAllOpenedForms = function ($event) {
        //    if ($event.toElement.tagName == "BUTTON" || $event.toElement.tagName == "A")
        //    {

            //   console.log("Utrni forme!!!");
        //    }

        //}

    }]);
});